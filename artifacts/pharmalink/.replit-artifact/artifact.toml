kind = "web"
previewPath = "/"
title = "PharmLink - منصة التوظيف الصيدلي"
version = "1.0.0"
id = "artifacts/pharmalink"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = ["/"]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/pharmalink run dev"

[services.env]
PORT = "5173"
BASE_PATH = "/"

[services.production]
publicDir = "artifacts/pharmalink/dist/public"
serve = "static"
build = ["pnpm", "--filter", "@workspace/pharmalink", "run", "build"]

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
